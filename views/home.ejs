<%- include('partials/header'); %> 


<h1>Hello</h1>
<br>

<% posts.forEach(function(post) { %>
    <h1><%= post.title %></h1>
    <span><%= post.content.substring(0,100)  + " ..."%> </span> <a href="/posts/<%= post.title %>"> Read More</a> <br>
<% }) %> 

<%- include('partials/footer'); %> 